'use strict'

class Rtc {
  /**
   * @param {Registry}registry
   */
  static create (registry) {

  }

  /**
   * Create a blob to receive data.
   * @param blobResource
   * @return {Promise<ArrayBuffer>}
   */
  static async receive (blobResource) {
  }

  /**
   * Create a blob to send data.
   * @param arrayBuffer
   * @return {BlobResource}
   */
  static wrap (arrayBuffer) {
  }
}

module.exports = Rtc
